{
    "watch": ["server/**/*.ts", "server/**/*.sql", "db/**/*.sql"],
    "ignore": ["server/service/**/*.ts"],
    "execMap": {
      "ts": "npm run codegen && ts-node --project tsconfig.server.json"
    },
    "env": {
      "NODE_ENV": "development"
    },
    "ext": "ts sql"
}